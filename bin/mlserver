#!/usr/bin/env ruby

require "MLserver"
h = MLserver::ClientHandler.new do |request, client|
  error = MLserver::ErrorResponse.new(404)
  client.puts error.response.to_s
end

MLserver::Server.start(port:5555, handler: h, logger: MLserver::DefaultLogger)
